const otpVerification: string = '{0} is your OTP for verification.';

const mfaVerification: string = '{0} is MFA code for user {1}.';

const loginOtpVerification: string = '{0} is your OTP for login.';

const Templates: Record<string, string> = {
    otpVerification,
    mfaVerification,
    loginOtpVerification,
};

export default Templates;
